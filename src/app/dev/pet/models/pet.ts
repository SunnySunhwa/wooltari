export interface Pet {
  count: number;
  next: null;
  previous: null;
  results: object;

  // name: string;
  // birth_date: string;
  // gender: string;
  // body_color: string;
  // identified_number: string;
  // is_neutering: boolean;
}
